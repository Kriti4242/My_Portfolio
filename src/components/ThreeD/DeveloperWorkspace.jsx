/**
 * DeveloperWorkspace — legacy wrapper kept for backward compatibility.
 * The actual 3D world is now rendered by DeveloperWorld (fixed position canvas).
 * This component renders nothing so Hero.jsx fallback still works.
 */
export default function DeveloperWorkspace() {
  return null;
}
